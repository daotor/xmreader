# XMReader 版本号规则

## 发布 Tag 格式

XMReader 的发布版本号统一使用以下格式：

`v{yy}.{MMdd}.{HHmm}`

示例：

- `v26.0411.1735`
- `v26.1231.2359`

## 字段说明

- `yy`：两位年份，例如 `2026 -> 26`
- `MMdd`：月日四位数字，例如 `4月11日 -> 0411`
- `HHmm`：24 小时制时分，例如 `17:35 -> 1735`

## 约束

- Git tag 必须以 `v` 开头
- 不再使用语义化版本号格式，例如 `v1.2.3`
- 不使用 `alpha`、`beta`、`rc` 等后缀
- GitHub Release、CI 构建版本、应用元数据版本都应以该规则为准

## 推荐生成方式

按本地时间生成当前时间戳版本号：

`v{yy}.{MMdd}.{HHmm}`
